
---
title: "Monthly Trends in COVID-19 Cases and Deaths in Massachusetts (2020-2021)"
output: 
  flexdashboard::flex_dashboard:
    orientation: columns
    vertical_layout: fill
---





```{r}
# Load necessary libraries
library(ggplot2)
library(dplyr)
library(reshape2)

# Load the dataset
link <- "https://github.com/DACSS-Visual/tabular_bivar_numnum/raw/main/data/covidMA.rds"
covidMA <- readRDS(url(link))

# Aggregate data by month (sum cases and deaths for each month)
covidMA_monthly <- aggregate(data = covidMA, cbind(cases, deaths) ~ yearMonth, sum)

# Convert yearMonth to Date format
covidMA_monthly$yearMonth <- as.Date(covidMA_monthly$yearMonth)

# Reshape data for ggplot compatibility (long format for cases and deaths)
covidMA_monthly_long <- melt(covidMA_monthly, 
                             variable.name = 'type',
                             value.name = 'value',
                             id.vars = 'yearMonth')

# Identify the worst month of the first wave (end of 2020)
worst_first_wave <- as.Date("2020-12-01")

# Identify the actual worst month based on highest case counts in the data
worst_month <- covidMA_monthly$yearMonth[which.max(covidMA_monthly$cases)]

# Updated Title, Subtitle, and Caption
titleText <- "COVID-19 Case Spike and Declining Deaths in MA"
sub_titleText <- "Data aggregated monthly (August 2020 to December 2021) for Massachusetts."
captionText <- "Source: MA - Covid19 Dashboard | Website: https://www.mass.gov/info-details/covid-19-reporting"

# Generate the plot with corrected vertical lines and annotations for both peaks
covidLines <- ggplot(covidMA_monthly_long, aes(x = yearMonth, y = value, color = type)) +
  geom_line(size = 1) +
  
  # Vertical line and annotation for the first wave (Dec 2020)
  geom_vline(xintercept = as.numeric(worst_first_wave), linetype = "dashed", color = "black", size = 1) +
  annotate("text", x = worst_first_wave, y = max(covidMA_monthly$value) / 2, 
           label = "First Wave Peak", angle = 90, vjust = -0.5, size = 4) +

  # Vertical line and annotation for the worst month (Dec 2021)
  geom_vline(xintercept = as.numeric(worst_month), linetype = "dotted", color = "red", size = 1) +
  annotate("text", x = worst_month, y = max(covidMA_monthly$value), 
           label = "Worst Month", angle = 90, vjust = -0.5, size = 4) +

  # Log transformation applied to Y-axis for better scaling
  scale_y_log10(labels = scales::comma) + 
  
  # Customizing the color and scales
  scale_x_date(date_breaks = "1 month", date_labels = "%b-%Y") +
  scale_color_manual(values = c("cases" = "blue", "deaths" = "red")) +
  
  # Adding labels and captions
  labs(title = titleText, 
       subtitle = sub_titleText, 
       caption = captionText, 
       x = "Date", 
       y = "Count (Log Scale)", 
       color = "Data Type") +

  # Adjusting the theme for better title and caption spacing
  theme_minimal() +
  theme(
    plot.title.position = "plot",  
    plot.caption = element_text(hjust = 0, size = 10),  
    axis.text.x = element_text(angle = 90)
  )

# Print the plot explicitly for Flexdashboard rendering
print(covidLines)

# Save the plot for future use in the dashboard
saveRDS(covidLines, file = "covidLines.rds")





```







